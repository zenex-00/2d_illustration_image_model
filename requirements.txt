# Core ML Libraries
# PyTorch 2.4.x/2.5.x required for RTX 4090 (compute capability sm_89) support
# 
# RTX 4090 Installation (REQUIRED for RTX 4090):
#   The RTX 4090 uses NVIDIA's Ada Lovelace architecture (sm_89) which requires:
#   - PyTorch 2.4.0+ with CUDA 12.1/12.4 support (stable, not nightly)
#   - Install from stable PyPI: pip install torch>=2.4.0,<2.6.0 torchvision>=0.19.0
#   - For CUDA 12.1: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#   - For CUDA 12.4: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
#   - xformers should be ENABLED for RTX 4090 (provides memory optimization)
#
# Note: RTX 4090 is fully supported by stable PyTorch 2.4.x+ with CUDA 12.1/12.4
torch>=2.4.0,<2.6.0
torchvision>=0.19.0
transformers>=4.56.2
diffusers>=0.30.0
accelerate>=0.33.0

# Computer Vision
opencv-python>=4.8.0
pillow>=10.0.0
scikit-image>=0.21.0
segment-anything>=1.0

# GroundingDINO
groundingdino-py>=0.4.0

# LaMa Inpainting
lama-cleaner>=1.2.0

# ControlNet
controlnet-aux>=0.0.10

# Background Removal
rembg>=2.0.69
onnxruntime>=1.16.0  # Required by rembg for ONNX model inference

# Depth Estimation
zoedepth>=0.1.0

# Upscaling
basicsr>=1.4.2
realesrgan>=0.3.0

# SVG Processing
svgpathtools>=1.4.4
shapely>=2.0.0

# Utilities
numpy>=1.24.0
scipy>=1.11.0
pyyaml>=6.0.1
python-dotenv>=1.0.0

# Error Handling & Retry
tenacity>=8.2.3

# Logging
structlog>=23.2.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0

# CLI
click>=8.1.7

# API
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
slowapi>=0.1.9
jinja2>=3.1.2

# Color Science (optional, for exact CIEDE2000)
colorspacious>=1.1.2

# GPU-accelerated computer vision
kornia>=0.7.0

# LoRA Training
peft>=0.13.0
albumentations>=1.3.0

# Memory Optimization (recommended for RTX 4090)
xformers>=0.0.23  # Enabled for RTX 4090 - provides memory-efficient attention
bitsandbytes>=0.43.0

# Monitoring (optional)
prometheus-client>=0.18.0

# HTTP requests (for model downloads)
requests>=2.31.0

